@page
@model UsersModel

<h2>Users</h2>

<a asp-page="Create">Create User</a>

<table>
    @foreach (var u in Model.Users)
    {
        <tr>
            <td>@u.FullName</td>
            <td>@u.Email</td>
        </tr>
    }
</table>